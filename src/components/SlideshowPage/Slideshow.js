import { useState } from 'react'

const Slideshow = () => {
  const[selected, setSelected] = useState("");
  const[close, setClose] = useState(false);

//   Need selected to display pic (perhaps an array of file paths that can
//     be cycled through)
//   Need close to toggle between open and close

  return (
    <div>
      
    </div>
  )
}

export default Slideshow
